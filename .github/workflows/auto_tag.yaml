name: auto-tag

on:
  check_suite:
    types: [completed]

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
    - name: echo
      shell: bash
      run: | 
        echo 123
        echo sha ${{ github.sha }}
        echo ref ${{ github.ref }}
