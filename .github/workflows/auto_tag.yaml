name: auto-tag

on:
  check_suite:
    types: [completed]

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
    - name: echo
      run: | 
        echo sha ${{ github.sha }}
        echo ref ${{ github.ref }}
