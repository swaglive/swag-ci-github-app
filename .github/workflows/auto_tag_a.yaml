name: debug

on:
  check_run:
    types: [requested]

jobs:
  debug:
    runs-on: ubuntu-latest

    steps:
    - name: Show access token
      run: |
        echo ${{github.sha}}
        echo ${{github.ref}}
      
